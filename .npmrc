# Pin the public registry so dependency changes made on a machine with a
# private mirror in ~/.npmrc cannot rewrite `resolved` URLs in the lockfile.
# CI has no access to those mirrors, and `npm ci` fails outright when it hits
# one. Project-level .npmrc takes precedence over the user-level file.
registry=https://registry.npmjs.org
